export const MANUAL = 'MANUAL'
export const LINKED = 'LINKED'
export const SCHEDULED = 'SCHEDULED'
export const UNTIL = 'UNTIL'

export const triggers = { MANUAL, LINKED, SCHEDULED, UNTIL }

export default { ...triggers }
